docker build -t php-mysql-crud .
  
docker run -d -p 8080:80 \
  -e DB_HOST=db_endpoint \
  -e DB_USER=user \
  -e DB_PASSWORD=password \
  -e DB_NAME=db \
  --name php-mysql-crud php-mysql-crud

  mysql -h phpdb.cp4oyg4e6tsj.ap-south-1.rds.amazonaws.com -u admin -p phpdb < employees_dump.sql
